package com.example.telkompdc.myapplication;

import android.os.Bundle;
import android.support.annotation.Nullable;
import android.support.v7.app.AppCompatActivity;
import android.util.Log;
import android.widget.Button;
import android.widget.TextView;
import android.widget.Toast;

/**
 * Created by TELKOMPDC on 10/31/2016.
 */

public class CounterActivity extends AppCompatActivity {
    @Override
   // protected void onCreate(@Nullable Bundle savedInstanceState) {
        //super.onCreate(savedInstanceState);
        //setContentView(R.layout.activity_main);
        //TextView counterNumber = (TextView) findViewById(R.id.button);
        //Button plus =(Button) findViewById(R.id.plus);
        //Button minus = (Button) findViewById(R.id.minus);
       // Log.v(LOG_TAG, "Angka sekarang nilainya: " + counterNumber);
     //   Toast.makeText(this, "Angka sekarang nilainya : " + counterNumber, Toast.LENGTH_LONG).show();

    }
}
